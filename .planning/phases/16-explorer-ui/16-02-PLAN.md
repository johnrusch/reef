---
phase: 16-explorer-ui
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified: []
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - GEN-01
  - GEN-02

must_haves:
  truths:
    - "User visually confirms sidebar tree shows 4 C4 levels and highlights the active one"
    - "User visually confirms breadcrumb bar updates on navigation"
    - "User visually confirms sidebar auto-highlights when drilling down via diagram element click"
    - "User visually confirms single Generate button creates all 4 levels"
    - "User visually confirms toolbar has exactly Regenerate and Show/Hide Changes"
  artifacts: []
  key_links: []
---

<objective>
Visual verification that the complete Phase 16 Explorer UI works correctly in the running application.

Purpose: Confirm that sidebar tree, breadcrumbs, toolbar controls, and generation flow all function as designed before marking the phase complete.

Output: User approval of the Explorer UI implementation.
</objective>

<execution_context>
@/Users/johnrusch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johnrusch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-explorer-ui/16-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of Explorer UI</name>
  <action>
    Present the user with verification steps for all 5 Phase 16 requirements.
    Start the app with `npm run dev` and guide user through testing.
  </action>
  <verify>User confirms all tests pass</verify>
  <done>User types "approved" confirming all 5 requirements verified</done>
  <what-built>
    Complete Phase 16 Explorer UI:
    1. C4 Hierarchy Sidebar Tree (NAV-01) — left panel showing Context/Containers/Components/Code with collapse toggle
    2. Breadcrumb Bar (NAV-02) — clickable ancestor trail showing current hierarchy position
    3. Sidebar Auto-Highlight (NAV-03) — tree highlights active node when drilling down via diagram click
    4. Single-Button Generation (GEN-01) — "Generate All Diagrams" button on first visit creates all 4 levels
    5. Minimal Toolbar (GEN-02) — exactly Regenerate + Show/Hide Changes toggle
  </what-built>
  <how-to-verify>
    Start the app with `npm run dev` and navigate to Visual Map tab.

    **Test 1 — Sidebar Tree (NAV-01):**
    1. Open a repository with existing diagrams
    2. Verify a sidebar tree appears on the LEFT side of the diagram
    3. Confirm it lists: "System Context", "Containers", "Components", "Code"
    4. Click "Containers" in the tree — diagram should switch to container level
    5. Click the collapse toggle — sidebar should collapse to icon-only mode
    6. Click again — sidebar should expand back to full labels

    **Test 2 — Breadcrumbs (NAV-02):**
    1. With a diagram showing, verify a breadcrumb bar appears below the toolbar
    2. Navigate to different C4 levels — breadcrumbs should update
    3. Click an ancestor in the breadcrumb — should navigate back up

    **Test 3 — Auto-Highlight (NAV-03):**
    1. While viewing the Context diagram, click a system element to drill down
    2. Verify the sidebar tree updates to highlight "Containers" as active
    3. Continue drilling down — verify sidebar tracks each level change

    **Test 4 — Generate All (GEN-01):**
    1. If possible, test with a repo that has no diagrams (or use a new repo)
    2. Verify a "Generate All Diagrams" button appears
    3. Click it — verify it generates diagrams for all 4 C4 levels (watch for progress/loading)

    **Test 5 — Toolbar (GEN-02):**
    1. Verify the toolbar has exactly two buttons: "Regenerate" and "Show/Hide Changes"
    2. No "Force Regenerate" button should exist
    3. Click "Show Changes" — button should toggle to "Hide Changes"
    4. Click "Regenerate" — confirm dialog should appear, then regeneration begins
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
User visual verification of all 5 Phase 16 requirements in the running application.
</verification>

<success_criteria>
User confirms all 5 tests pass with no visual regressions or broken functionality.
</success_criteria>

<output>
After completion, create `.planning/phases/16-explorer-ui/16-02-SUMMARY.md`
</output>
