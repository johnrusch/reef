---
phase: 05-persistent-storage-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/main/services/storageService.test.ts
  - tests/unit/main/services/migrationService.test.ts
  - tests/integration/storageService.test.ts
  - tests/integration/migrationService.test.ts
  - tests/unit/renderer/stores/diagramStateStore.test.ts
  - tests/unit/renderer/components/DiagramStateBadge.test.tsx
  - tests/fixtures/v1_cache.db
  - tests/mocks/electron.mock.ts
autonomous: true
requirements:
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04

must_haves:
  truths:
    - "Test scaffolds exist for all STOR requirements before implementation"
    - "Unit tests define expected behavior via describe/it blocks"
    - "Integration tests define persistence and migration flows"
    - "Test fixtures provide sample v1.0 database for migration testing"
  artifacts:
    - path: "tests/unit/main/services/storageService.test.ts"
      provides: "Storage service unit test scaffold"
      contains: "describe\\('C4StorageService'"
    - path: "tests/unit/main/services/migrationService.test.ts"
      provides: "Migration service unit test scaffold"
      contains: "describe\\('MigrationService'"
    - path: "tests/integration/storageService.test.ts"
      provides: "Storage integration test scaffold"
      contains: "describe\\('StorageService Integration'"
    - path: "tests/integration/migrationService.test.ts"
      provides: "Migration integration test scaffold"
      contains: "describe\\('MigrationService Integration'"
    - path: "tests/unit/renderer/stores/diagramStateStore.test.ts"
      provides: "Zustand store test scaffold"
      contains: "describe\\('diagramStateStore'"
    - path: "tests/unit/renderer/components/DiagramStateBadge.test.tsx"
      provides: "Badge component test scaffold"
      contains: "describe\\('DiagramStateBadge'"
  key_links:
    - from: "tests/fixtures/v1_cache.db"
      to: "tests/unit/main/services/migrationService.test.ts"
      via: "test data import"
      pattern: "v1_cache\\.db"
---

<objective>
Create test scaffolds for all Phase 5 requirements before implementation begins.

Purpose: Satisfy Nyquist sampling requirement - behavioral tests must exist before tasks that implement the behavior. Test scaffolds define expected behavior via describe/it blocks with placeholder implementations (test.todo or expect.fail).

Output: Test files with structure matching RESEARCH.md lines 650-678, ready for implementation plans to fill in.
</objective>

<execution_context>
@/Users/johnrusch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johnrusch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-persistent-storage-foundation/05-CONTEXT.md
@.planning/phases/05-persistent-storage-foundation/05-RESEARCH.md
@vitest.config.ts
@vitest.config.main.ts
@tests/unit/main/services/gitService.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create storage service test scaffolds</name>
  <files>
    tests/unit/main/services/storageService.test.ts
    tests/integration/storageService.test.ts
  </files>
  <action>
Create test scaffolds for C4StorageService that define expected behavior:

**tests/unit/main/services/storageService.test.ts:**

```typescript
import { describe, it, expect, beforeEach, afterEach } from 'vitest';

describe('C4StorageService', () => {
  describe('diagram persistence (STOR-01)', () => {
    it.todo('stores diagram without TTL expiration');
    it.todo('retrieves diagram regardless of age (no TTL)');
    it.todo('returns null for non-existent diagram');
    it.todo('updates existing diagram via INSERT OR REPLACE');
    it.todo('normalizes repo paths to forward slashes');
  });

  describe('WAL mode (STOR-03)', () => {
    it.todo('enables WAL journal mode on initialization');
    it.todo('sets synchronous mode to FULL');
    it.todo('configures wal_autocheckpoint');
  });

  describe('state tracking (STOR-04)', () => {
    it.todo('stores state column with diagram');
    it.todo('retrieves state for existing diagram');
    it.todo('returns never_generated for unknown diagram');
    it.todo('updates state without replacing diagram content');
    it.todo('stores error message with error state');
    it.todo('enforces valid state values via CHECK constraint');
  });

  describe('storage operations', () => {
    it.todo('getAllDiagramsForRepo returns all diagrams for repo');
    it.todo('clearAllDiagrams removes all entries');
    it.todo('getStorageStats returns path, size, and count');
    it.todo('close properly closes database connection');
  });

  describe('corruption handling', () => {
    it.todo('detects corruption via integrity_check pragma');
    it.todo('creates backup of corrupted database');
    it.todo('initializes fresh database after corruption');
  });
});
```

**tests/integration/storageService.test.ts:**

```typescript
import { describe, it, expect, beforeEach, afterEach } from 'vitest';

describe('StorageService Integration', () => {
  describe('persistence across restarts (STOR-01)', () => {
    it.todo('diagram survives service close and reopen');
    it.todo('state persists after service restart');
  });

  describe('concurrent access (STOR-03)', () => {
    it.todo('allows concurrent reads during write operation');
    it.todo('maintains data consistency under concurrent access');
  });
});
```

Use existing test patterns from tests/unit/main/services/gitService.test.ts for structure.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/services/storageService.test.ts tests/integration/storageService.test.ts --passWithNoTests 2>&1 | grep -E "(PASS|FAIL|todo)" || echo "Test files created"</automated>
  </verify>
  <done>Storage service test scaffolds exist with .todo tests for STOR-01, STOR-03, STOR-04 behaviors</done>
</task>

<task type="auto">
  <name>Task 2: Create migration service test scaffolds</name>
  <files>
    tests/unit/main/services/migrationService.test.ts
    tests/integration/migrationService.test.ts
    tests/fixtures/v1_cache.db
    tests/mocks/electron.mock.ts
  </files>
  <action>
Create test scaffolds for MigrationService plus required fixtures:

**tests/unit/main/services/migrationService.test.ts:**

```typescript
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';

describe('MigrationService', () => {
  describe('version detection (STOR-02)', () => {
    it.todo('detects v1.0 database via user_version = 0');
    it.todo('detects v1.1 database via user_version = 1');
    it.todo('returns needsMigration true when v1.0 cache exists');
    it.todo('returns needsMigration false after migration complete');
  });

  describe('diagram migration (STOR-02)', () => {
    it.todo('copies all v1.0 diagrams to v1.1 schema');
    it.todo('marks expired TTL diagrams as stale');
    it.todo('marks fresh TTL diagrams as fresh');
    it.todo('extracts level from v1.0 diagram_type field');
    it.todo('handles null element_id for root diagrams');
    it.todo('sets user_version to 1 after migration');
  });

  describe('TTL expiration detection', () => {
    it.todo('detects expired context diagrams (>7 days)');
    it.todo('detects expired container diagrams (>3 days)');
    it.todo('detects expired component diagrams (>1 day)');
    it.todo('detects expired code diagrams (>6 hours)');
  });

  describe('migration safety', () => {
    it.todo('uses transaction for atomic migration');
    it.todo('continues migration if single diagram fails');
    it.todo('logs warning for failed diagram migration');
    it.todo('checks migration lock to prevent concurrent migrations');
  });

  describe('cleanup', () => {
    it.todo('cleanupV1Cache removes v1.0 database file');
    it.todo('cleanupV1Cache removes WAL files');
  });
});
```

**tests/integration/migrationService.test.ts:**

```typescript
import { describe, it, expect, beforeEach, afterEach } from 'vitest';

describe('MigrationService Integration', () => {
  describe('end-to-end migration (STOR-02)', () => {
    it.todo('migrates fixture v1.0 database to v1.1');
    it.todo('deletes v1.0 cache after successful migration');
    it.todo('preserves diagram content through migration');
    it.todo('correctly calculates staleness from timestamps');
  });
});
```

**tests/fixtures/v1_cache.db:**
Create a minimal SQLite database with v1.0 schema using the following SQL:

```sql
-- Create v1.0 schema matching c4CacheService.ts structure
CREATE TABLE diagram_cache (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  repo_path TEXT NOT NULL,
  diagram_type TEXT NOT NULL,
  element_id TEXT,
  diagram_content TEXT NOT NULL,
  created_at TEXT NOT NULL,
  last_accessed TEXT NOT NULL,
  access_count INTEGER DEFAULT 1,
  UNIQUE(repo_path, diagram_type, element_id)
);

-- Insert 3 sample entries for TTL testing:
-- Entry 1: Fresh context diagram (created 1 day ago, within 7-day TTL)
INSERT INTO diagram_cache (repo_path, diagram_type, element_id, diagram_content, created_at, last_accessed, access_count)
VALUES ('/test/repo1', 'context', NULL, 'graph TD; A-->B', datetime('now', '-1 day'), datetime('now', '-1 day'), 1);

-- Entry 2: Expired context diagram (created 10 days ago, outside 7-day TTL)
INSERT INTO diagram_cache (repo_path, diagram_type, element_id, diagram_content, created_at, last_accessed, access_count)
VALUES ('/test/repo2', 'context', NULL, 'graph TD; C-->D', datetime('now', '-10 days'), datetime('now', '-10 days'), 2);

-- Entry 3: Expired code diagram (created 12 hours ago, outside 6-hour TTL)
INSERT INTO diagram_cache (repo_path, diagram_type, element_id, diagram_content, created_at, last_accessed, access_count)
VALUES ('/test/repo1', 'code', 'AuthService', 'classDiagram; class Auth', datetime('now', '-12 hours'), datetime('now', '-12 hours'), 1);
```

Execute this SQL to create the fixture database file.

**tests/mocks/electron.mock.ts:**

```typescript
import { vi } from 'vitest';

export const mockElectronApp = {
  getPath: vi.fn((name: string) => {
    if (name === 'userData') return '/tmp/test-user-data';
    return '/tmp/test';
  }),
};

export const mockElectronStore = {
  get: vi.fn(),
  set: vi.fn(),
};

// Export default mock setup function
export function setupElectronMocks() {
  vi.mock('electron', () => ({
    app: mockElectronApp,
  }));
}
```

Reference existing test patterns and fixtures structure from tests/fixtures/ directory.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/services/migrationService.test.ts tests/integration/migrationService.test.ts --passWithNoTests 2>&1 | grep -E "(PASS|FAIL|todo)" || echo "Test files created" && test -f tests/fixtures/v1_cache.db && echo "Fixture exists"</automated>
  </verify>
  <done>Migration service test scaffolds exist with .todo tests for STOR-02, v1.0 fixture database created with explicit schema (diagram_type, created_at, last_accessed, access_count columns) and 3 sample entries for TTL testing</done>
</task>

<task type="auto">
  <name>Task 3: Create frontend test scaffolds</name>
  <files>
    tests/unit/renderer/stores/diagramStateStore.test.ts
    tests/unit/renderer/components/DiagramStateBadge.test.tsx
  </files>
  <action>
Create test scaffolds for frontend state management and components:

**tests/unit/renderer/stores/diagramStateStore.test.ts:**

```typescript
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';

describe('diagramStateStore', () => {
  describe('state retrieval', () => {
    it.todo('returns never_generated for unknown entries');
    it.todo('returns stored state for known entries');
    it.todo('generates consistent keys from repoPath/level/elementId');
  });

  describe('state mutations (STOR-04)', () => {
    it.todo('setState creates new entry in store');
    it.todo('setState updates existing entry');
    it.todo('setState normalizes path separators');
  });

  describe('transition helpers', () => {
    it.todo('transitionToGenerating sets generating state');
    it.todo('transitionToFresh sets fresh state');
    it.todo('transitionToStale sets stale state');
    it.todo('transitionToError sets error state with message');
  });

  describe('bulk operations', () => {
    it.todo('loadStatesFromBackend replaces all states');
    it.todo('clearStatesForRepo removes matching entries');
  });
});
```

**tests/unit/renderer/components/DiagramStateBadge.test.tsx:**

```typescript
import { describe, it, expect, vi } from 'vitest';

describe('DiagramStateBadge', () => {
  describe('icon rendering (STOR-04)', () => {
    it.todo('renders null for never_generated state');
    it.todo('renders green checkmark for fresh state');
    it.todo('renders amber clock for stale state');
    it.todo('renders blue spinner for generating state');
    it.todo('renders red warning for error state');
  });

  describe('interactivity', () => {
    it.todo('stale badge is clickable and calls onRegenerate');
    it.todo('error badge is clickable and calls onRegenerate');
    it.todo('fresh badge shows tooltip on hover');
    it.todo('error badge shows error message in tooltip');
  });

  describe('accessibility', () => {
    it.todo('buttons have accessible labels');
    it.todo('spinner has aria-label for screen readers');
  });
});
```

Use existing component test patterns from tests/unit/renderer/ directory.
Import @testing-library/react for component rendering.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/renderer/stores/diagramStateStore.test.ts tests/unit/renderer/components/DiagramStateBadge.test.tsx --passWithNoTests 2>&1 | grep -E "(PASS|FAIL|todo)" || echo "Test files created"</automated>
  </verify>
  <done>Frontend test scaffolds exist with .todo tests for STOR-04 state management and badge rendering</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. All test scaffold files exist in expected locations
2. `npm run test:unit` runs without errors (tests are .todo so they skip)
3. Test descriptions match RESEARCH.md requirement mappings
4. v1_cache.db fixture has valid v1.0 schema
5. electron.mock.ts provides app.getPath mock
</verification>

<success_criteria>
- [ ] tests/unit/main/services/storageService.test.ts exists with STOR-01, STOR-03, STOR-04 test stubs
- [ ] tests/unit/main/services/migrationService.test.ts exists with STOR-02 test stubs
- [ ] tests/integration/storageService.test.ts exists with persistence test stubs
- [ ] tests/integration/migrationService.test.ts exists with migration flow test stubs
- [ ] tests/unit/renderer/stores/diagramStateStore.test.ts exists with state machine test stubs
- [ ] tests/unit/renderer/components/DiagramStateBadge.test.tsx exists with badge rendering test stubs
- [ ] tests/fixtures/v1_cache.db exists with v1.0 schema sample data
- [ ] tests/mocks/electron.mock.ts provides app.getPath mock
- [ ] `npm run test:unit` passes (skips .todo tests)
</success_criteria>

<output>
After completion, create `.planning/phases/05-persistent-storage-foundation/05-00-SUMMARY.md`
</output>
