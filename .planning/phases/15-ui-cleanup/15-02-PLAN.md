---
phase: 15-ui-cleanup
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified: []
autonomous: false
requirements: [UICL-01, UICL-02, UICL-03, UICL-04]

must_haves:
  truths:
    - "User opens Visual Map tab and sees no configuration landing page"
    - "User viewing a diagram sees no legacy toolbar controls"
    - "User viewing a diagram sees no DiagramInfo sidebar"
    - "User sees no Beta badge on the Visual Map tab label"
    - "User can still regenerate diagrams via Regenerate button"
    - "User still sees GeneratePromptCard for repos without diagrams"
  artifacts: []
  key_links: []
---

<objective>
Visually verify all four UICL removals in the running application.

Purpose: Confirm that the automated code removals produce the correct visual result — no settings page, no legacy toolbar, no sidebar, no Beta badge — and that the remaining UI (Regenerate buttons, breadcrumbs, diagram panel, GeneratePromptCard) still works correctly.
Output: Human confirmation that Phase 15 success criteria are met.
</objective>

<execution_context>
@/Users/johnrusch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johnrusch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-ui-cleanup/15-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of all four UICL removals</name>
  <files></files>
  <action>
    Start the dev server with `npm run dev` and present the following verification checklist to the user.

    What was built: Removed all four categories of legacy UI — configuration landing page (UICL-01), legacy toolbar controls (UICL-02), DiagramInfo sidebar (UICL-03), and Beta badge (UICL-04).

    How to verify:
    1. Run `npm run dev` to start the application
    2. Open any repository in the Reef desktop client

    UICL-04 — Beta badge removed:
    3. Look at the tab bar — the "Visual Map" tab should have NO "Beta" badge next to its label

    UICL-01 — Settings landing page removed:
    4. Click the "Visual Map" tab for a repository that has never had a diagram generated
    5. You should see the GeneratePromptCard (single "Generate" button) — NOT the old settings page with C4 level picker, detail level slider, focus area toggles, AI model selector, feature info cards, or "Traditional File Tree" button

    UICL-02 — Legacy toolbar removed:
    6. If a diagram exists (or generate one), look at the toolbar above the diagram
    7. You should see ONLY "Regenerate" and "Force Regenerate" buttons — NO Component/Class/Sequence type buttons, NO detail level slider, NO focus area toggles, NO "Show Changes" button

    UICL-03 — DiagramInfo sidebar removed:
    8. While viewing a diagram, look at the right side of the diagram view
    9. There should be NO sidebar showing generation metadata, cost, token count, cache status, or "Clear Cache" button
    10. The diagram panel should take the full width

    Functional check:
    11. Click "Regenerate" — confirm the confirmation dialog appears
    12. Confirm or cancel the dialog — either action should work correctly
    13. If breadcrumbs are visible (for drill-down navigation), confirm they still work

    Resume signal: Type "approved" if all checks pass, or describe any issues found.
  </action>
  <verify>
    <automated>cd /Users/johnrusch/Code/reef && npm run typecheck && npm run test:unit -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>User has visually confirmed all four UICL removals are correct in the running application, and no visual regressions exist.</done>
</task>

</tasks>

<verification>
Human visual inspection confirms all four UICL requirements are satisfied in the running application.
</verification>

<success_criteria>
- All four UICL removals visually confirmed by user
- No unexpected visual regressions
- Regenerate functionality still works
- GeneratePromptCard still appears for repos without diagrams
</success_criteria>

<output>
After completion, create `.planning/phases/15-ui-cleanup/15-02-SUMMARY.md`
</output>
