---
phase: 01-c4-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/components/tabs/VisualMapTab.tsx
autonomous: true
gap_closure: true
requirements: [NAV-06]

must_haves:
  truths:
    - "User can select container when generating Component diagrams"
    - "User can select component when generating Code diagrams"
    - "Component diagram generation passes elementId to backend"
    - "Code diagram generation passes elementId to backend"
  artifacts:
    - path: "src/renderer/components/tabs/VisualMapTab.tsx"
      provides: "Container and component selection dropdowns"
      min_lines: 650
  key_links:
    - from: "VisualMapTab.tsx container dropdown"
      to: "generateDiagram() call"
      via: "elementId parameter"
      pattern: "elementId.*options"
    - from: "VisualMapTab.tsx component dropdown"
      to: "generateDiagram() call"
      via: "elementId parameter"
      pattern: "elementId.*options"
---

<objective>
Add UI controls to collect elementId for Component and Code diagram generation

**Purpose:** Enable users to generate Component and Code diagrams by selecting container/component scope

**Output:** VisualMapTab with dropdown selectors for drill-down context
</objective>

<execution_context>
@/Users/johnrusch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johnrusch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-c4-foundation/01-UAT.md
@.planning/phases/01-c4-foundation/01-03-SUMMARY.md
@src/renderer/components/tabs/VisualMapTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Add elementId selection UI for Component and Code diagrams</name>
  <files>src/renderer/components/tabs/VisualMapTab.tsx</files>
  <action>
Add UI controls to collect elementId for Component and Code diagrams:

**Add state for elementId:**
- Add `const [elementId, setElementId] = useState<string | undefined>(undefined)` after existing state declarations
- Reset elementId when diagram type changes to non-drill-down types

**Add container selection dropdown (appears when c4-component selected):**
- After the "C4 Architecture Level" section (around line 384)
- Conditional render: `{diagramType === 'c4-component' && (...)`
- Label: "Select Container to Analyze"
- Dropdown with options: "Main Process", "Renderer Process", "Preload Script"
- Update elementId state when selected
- Store values: "reef_main", "reef_renderer", "reef_preload" (sanitized IDs matching backend)

**Add component selection dropdown (appears when c4-code selected):**
- After container dropdown section
- Conditional render: `{diagramType === 'c4-code' && (...)`
- Label: "Select Component to Analyze"
- Dropdown with common components: "GitService", "GitHubService", "C4AnalyzerService", "StaticAnalyzerService", "DiagramGeneratorService"
- Update elementId state when selected
- Store values as sanitized IDs (lowercase with underscores)

**Update generateDiagram call (line 131):**
- Pass elementId in options when calling window.reef.diagram.generate
- Add to options object: `elementId: elementId`
- Only passed when elementId is defined (Component/Code diagrams)

**Styling:**
- Match existing dropdown style (bg-gray-700, hover:bg-gray-600)
- Use grid layout for dropdown items
- Add helper text explaining drill-down context

**Why this approach:**
- Backend c4PlantUMLGenerator.ts requires elementId for Component/Code levels (validated in 01-03)
- Container names must match PlantUML IDs generated by backend (Main Process → reef_main)
- Progressive disclosure: dropdowns only appear for relevant diagram types
  </action>
  <verify>
Manual test:
1. Select "Component" diagram type → container dropdown appears
2. Select "Code" diagram type → component dropdown appears
3. Select "Context" diagram type → no extra dropdowns
4. TypeScript compilation succeeds: `npm run typecheck`
  </verify>
  <done>
- Container dropdown appears when c4-component selected
- Component dropdown appears when c4-code selected
- Dropdowns hidden for Context/Container diagram types
- elementId passed to backend when generating Component/Code diagrams
- TypeScript compilation succeeds
  </done>
</task>

</tasks>

<verification>
1. Start dev server: `npm run dev`
2. Open Reef application
3. Navigate to repository Visual Map tab
4. Test Component diagram:
   - Select "Component" diagram type
   - Verify container dropdown appears
   - Select "Main Process"
   - Click "Generate Diagram"
   - Verify no "elementId required" error
5. Test Code diagram:
   - Select "Code" diagram type
   - Verify component dropdown appears
   - Select "GitService"
   - Click "Generate Diagram"
   - Verify no "elementId required" error
6. Test other diagram types:
   - Select "Context" diagram type
   - Verify no extra dropdowns
   - Select "Container" diagram type
   - Verify no extra dropdowns
</verification>

<success_criteria>
- User can generate Component diagram by selecting container from dropdown
- User can generate Code diagram by selecting component from dropdown
- Context and Container diagrams do not require elementId selection
- No "PlantUML generation failed: Component diagram requires elementId" errors
- No "PlantUML generation failed: Code diagram requires elementId" errors
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-c4-foundation/01-04-SUMMARY.md`
</output>
