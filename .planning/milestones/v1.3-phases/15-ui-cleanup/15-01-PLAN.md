---
phase: 15-ui-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/renderer/components/repository/RepositoryTabs.test.tsx
  - tests/unit/renderer/components/DiagramViewer/DiagramControls.test.tsx
  - tests/unit/renderer/components/DiagramViewer/DiagramViewer.uicl.test.tsx
  - tests/unit/renderer/components/tabs/VisualMapTab.test.tsx
  - src/renderer/components/repository/RepositoryTabs.tsx
  - src/renderer/components/DiagramViewer/DiagramInfo.tsx
  - src/renderer/components/DiagramViewer/DiagramControls.tsx
  - src/renderer/components/DiagramViewer/DiagramViewer.tsx
  - src/renderer/components/tabs/VisualMapTab.tsx
autonomous: true
requirements: [UICL-01, UICL-02, UICL-03, UICL-04]

must_haves:
  truths:
    - "VisualMapTab renders no configuration landing page — no C4 level picker, detail level slider, focus area toggles, AI model selector, feature info cards, or file tree button"
    - "DiagramViewer toolbar shows no legacy Component/Class/Sequence type buttons, no detail level slider, no focus area toggles"
    - "DiagramViewer shows no DiagramInfo sidebar — no generation metadata, cost, tokens, or cache controls"
    - "Visual Map tab label shows no Beta badge"
    - "Regenerate and Force Regenerate buttons still work in DiagramControls"
    - "VisualMapTab still renders DiagramViewer for repos with stored diagrams"
    - "VisualMapTab still renders GeneratePromptCard for repos with never_generated state"
    - "VisualMapTab still renders API key modal when not configured"
  artifacts:
    - path: "tests/unit/renderer/components/repository/RepositoryTabs.test.tsx"
      provides: "UICL-04 test — no Beta badge rendered"
    - path: "tests/unit/renderer/components/DiagramViewer/DiagramControls.test.tsx"
      provides: "UICL-02 test — no legacy type buttons, slider, or focus toggles"
    - path: "tests/unit/renderer/components/DiagramViewer/DiagramViewer.uicl.test.tsx"
      provides: "UICL-03 test — no DiagramInfo sidebar rendered"
    - path: "tests/unit/renderer/components/tabs/VisualMapTab.test.tsx"
      provides: "UICL-01 test — no settings configuration page"
    - path: "src/renderer/components/repository/RepositoryTabs.tsx"
      provides: "Tab config without beta: true"
    - path: "src/renderer/components/DiagramViewer/DiagramControls.tsx"
      provides: "Gutted toolbar with only Regenerate + Force Regenerate + confirm dialog"
    - path: "src/renderer/components/DiagramViewer/DiagramViewer.tsx"
      provides: "DiagramViewer without DiagramInfo sidebar, with cleaned-up DiagramControls props"
    - path: "src/renderer/components/tabs/VisualMapTab.tsx"
      provides: "VisualMapTab without settings landing page"
  key_links:
    - from: "src/renderer/components/DiagramViewer/DiagramViewer.tsx"
      to: "src/renderer/components/DiagramViewer/DiagramControls.tsx"
      via: "DiagramControls props must match gutted interface"
      pattern: "<DiagramControls"
    - from: "src/renderer/components/tabs/VisualMapTab.tsx"
      to: "src/renderer/components/DiagramViewer/DiagramViewer.tsx"
      via: "DiagramViewer still rendered for viewMode === diagram"
      pattern: "<DiagramViewer"
    - from: "src/renderer/components/tabs/VisualMapTab.tsx"
      to: "generateDiagram function"
      via: "State vars diagramType, detailLevel, focusArea, modelType still feed generateDiagram"
      pattern: "generateDiagram"
---

<objective>
Remove all legacy UI controls from the diagram view: the configuration landing page, legacy toolbar buttons, DiagramInfo sidebar, and Beta badge.

Purpose: Clear the canvas for the Phase 16 explorer UI by eliminating all configuration and metadata clutter accumulated during v1.0-v1.2 development.
Output: Clean diagram view with only essential controls (Regenerate buttons, breadcrumbs, diagram panel).
</objective>

<execution_context>
@/Users/johnrusch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johnrusch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ui-cleanup/15-RESEARCH.md

<!-- Source files to modify -->
@src/renderer/components/repository/RepositoryTabs.tsx
@src/renderer/components/DiagramViewer/DiagramControls.tsx
@src/renderer/components/DiagramViewer/DiagramInfo.tsx
@src/renderer/components/DiagramViewer/DiagramViewer.tsx
@src/renderer/components/tabs/VisualMapTab.tsx

<interfaces>
<!-- Key types the executor needs from DiagramViewer.tsx -->

From src/renderer/components/DiagramViewer/DiagramViewer.tsx:
```typescript
export type DiagramType = 'component' | 'class' | 'sequence' | 'c4-context' | 'c4-container' | 'c4-component' | 'c4-code';
export type DetailLevel = 'overview' | 'architectural' | 'detailed';
export type FocusArea = 'api' | 'database' | 'business-logic' | 'auth' | undefined;
export type ModelType = 'haiku' | 'sonnet' | 'opus';

export interface DiagramMetadata {
  tokensUsed?: { input: number; output: number };
  generatedAt: string;
  diagramType: DiagramType;
  detailLevel: DetailLevel;
  focusArea?: FocusArea;
  repository: string;
  model: ModelType;
  generationTime: number;
  estimatedCost: number;
  cached: boolean;
  lastUpdated: string;
}
```

From src/renderer/components/DiagramViewer/DiagramControls.tsx (CURRENT — will be gutted):
```typescript
interface DiagramControlsProps {
  currentType: DiagramType;
  currentDetailLevel: DetailLevel;
  currentFocusArea?: FocusArea;
  showChanges?: boolean;
  isGenerating: boolean;
  hasChangedFiles: boolean;
  onTypeChange: (type: DiagramType) => void;
  onDetailLevelChange: (level: DetailLevel) => void;
  onFocusAreaChange: (area: FocusArea | undefined) => void;
  onShowChangesToggle?: (enabled: boolean) => void;
  onRegenerate: () => void;
  onForceRegenerate?: () => void;
}
```

AFTER gutting, DiagramControlsProps should become:
```typescript
interface DiagramControlsProps {
  isGenerating: boolean;
  onRegenerate: () => void;
  onForceRegenerate?: () => void;
}
```

From src/renderer/components/repository/RepositoryTabs.tsx:
```typescript
interface Tab {
  id: 'commit' | 'management' | 'visualmap';
  label: string;
  icon: React.ElementType;
  beta?: boolean;  // <-- to be removed
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Wave 0 tests for all four UICL requirements</name>
  <files>
    tests/unit/renderer/components/repository/RepositoryTabs.test.tsx,
    tests/unit/renderer/components/DiagramViewer/DiagramControls.test.tsx,
    tests/unit/renderer/components/DiagramViewer/DiagramViewer.uicl.test.tsx,
    tests/unit/renderer/components/tabs/VisualMapTab.test.tsx
  </files>
  <behavior>
    UICL-04 (RepositoryTabs.test.tsx):
    - Test: RepositoryTabs renders Visual Map tab without Beta badge text
    - Test: RepositoryTabs renders all three tab labels (Commit Workflow, Repository, Visual Map)

    UICL-02 (DiagramControls.test.tsx):
    - Test: DiagramControls does NOT render text "Diagram Type" label
    - Test: DiagramControls does NOT render text "Detail Level" label
    - Test: DiagramControls does NOT render text "Focus Area" label
    - Test: DiagramControls renders "Regenerate" button
    - Test: DiagramControls renders "Force Regenerate" button when onForceRegenerate provided
    - Test: DiagramControls shows confirm dialog on Regenerate click

    UICL-03 (DiagramViewer.uicl.test.tsx):
    - Test: DiagramViewer does NOT render "Diagram Information" heading text
    - Test: DiagramViewer does NOT import or render DiagramInfo component

    UICL-01 (VisualMapTab.test.tsx):
    - Test: VisualMapTab does NOT render "Diagram Settings" heading when in default state
    - Test: VisualMapTab does NOT render "AI Model" label text
    - Test: VisualMapTab does NOT render "Traditional File Tree" button text
  </behavior>
  <action>
    Create four test files in the test directory structure following existing test patterns in tests/unit/renderer/components/.

    For each test file, mock the required dependencies:
    - Mock `window.reef` API (c4Storage, diagram, git, cache, fileWatcher) with vi.fn() stubs
    - Mock lucide-react icons as simple spans
    - Mock child components that are not under test (e.g., mock DiagramPanel, DiagramBreadcrumbs for DiagramViewer tests)
    - Mock stores (navigationStore, diagramStateStore, diagramNavigationStore, repositoryStore)
    - Mock plantuml-encoder for VisualMapTab tests
    - Mock react-hotkeys-hook for DiagramViewer tests

    Testing approach per file:

    **RepositoryTabs.test.tsx:** Render RepositoryTabs with activeTab="visualmap" and assert no "Beta" text in document. Assert all three tab labels present.

    **DiagramControls.test.tsx:** Render DiagramControls with minimal required props (isGenerating=false, onRegenerate=vi.fn()). Assert "Diagram Type", "Detail Level", "Focus Area" labels are NOT in the document. Assert "Regenerate" button IS present. Test confirm dialog appears on click.

    **DiagramViewer.uicl.test.tsx:** Render DiagramViewer with required props (diagram content, metadata, etc.). Assert "Diagram Information" heading text is NOT in document. This validates DiagramInfo is not rendered.

    **VisualMapTab.test.tsx:** Render VisualMapTab with a repository prop. Mock checkConfiguration to return { configured: true }. Mock getDiagram/getSvg to return null (no stored diagram). Mock getRepoStates to return []. Assert "Diagram Settings" and "AI Model" and "Traditional File Tree" text are NOT in document.

    IMPORTANT: These tests should FAIL initially (before Task 2 makes the code changes). Run `npm run test:unit -- --reporter=verbose` to confirm RED state. Some tests may pass already if they test for absence of elements that are conditional — that's OK. The key tests that MUST fail are:
    - DiagramControls tests for absence of "Diagram Type" / "Detail Level" / "Focus Area" (these labels exist in current code)
    - DiagramViewer test for absence of "Diagram Information" (DiagramInfo currently renders this)

    Note: VisualMapTab tests for absence of "Diagram Settings" will depend on what render branch the component takes. The settings page (lines 604-985) only renders when viewMode is 'settings' AND no other early return matches. Since viewMode initializes to 'settings' and no stored diagram is found, the component will fall through to the settings page. So the test asserting "Diagram Settings" is NOT present WILL fail (RED state confirmed).
  </action>
  <verify>
    <automated>cd /Users/johnrusch/Code/reef && npm run test:unit -- --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>Four test files exist and are loadable by vitest. Tests that check for absence of removed UI elements initially fail (RED), confirming they are testing real behavior. Tests that check for presence of kept elements (Regenerate button, tab labels) pass.</done>
</task>

<task type="auto">
  <name>Task 2: Execute all four UI removals across 5 source files</name>
  <files>
    src/renderer/components/repository/RepositoryTabs.tsx,
    src/renderer/components/DiagramViewer/DiagramControls.tsx,
    src/renderer/components/DiagramViewer/DiagramViewer.tsx,
    src/renderer/components/tabs/VisualMapTab.tsx
  </files>
  <action>
    Execute the four removals in dependency order. After each file edit, run `npm run typecheck` to catch any prop threading breakage before moving to the next file.

    **File 1 — RepositoryTabs.tsx (UICL-04):**
    - Line 20: Remove `beta: true` from the visualmap tab object
    - Remove `beta?: boolean` from the Tab interface (line 8)
    - Remove the beta badge JSX: the `{tab.beta && (...)}` block (lines 48-52)
    - Result: Tab interface has only id, label, icon. No beta rendering logic.

    **File 2 — DiagramControls.tsx (UICL-02):**
    Gut the component to keep ONLY Regenerate + Force Regenerate buttons + confirm dialog.
    - Remove props from interface: `currentType`, `currentDetailLevel`, `currentFocusArea`, `showChanges`, `hasChangedFiles`, `onTypeChange`, `onDetailLevelChange`, `onFocusAreaChange`, `onShowChangesToggle`. Keep: `isGenerating`, `onRegenerate`, `onForceRegenerate`.
    - Remove destructured props from function signature to match.
    - Remove const arrays: `diagramTypes`, `detailLevels`, `focusAreas`.
    - Remove JSX: the entire left side `<div className="flex items-center gap-6">` containing the three control groups (Diagram Type buttons, Detail Level slider, Focus Area toggles, Show Changes button). This is lines 68-170.
    - Keep: the right-side `<div className="flex items-center gap-3">` with Regenerate + Force Regenerate buttons (lines 172-191). But restructure the outer flex container since the left side is gone — the buttons can be the only content.
    - Keep: the confirm dialog JSX (lines 195-224) and its state (showConfirmDialog).
    - Remove unused imports: `Sliders`, `Eye`, `EyeOff`. Keep: `RefreshCw`, `AlertCircle`.
    - Remove the type imports that are no longer needed: `DiagramType`, `DetailLevel`, `FocusArea` from DiagramViewer.
    - Result: A minimal toolbar with Regenerate, Force Regenerate, and confirm dialog.

    **File 3 — DiagramViewer.tsx (UICL-02 + UICL-03):**
    - Remove import of `DiagramInfo` (line 5): `import { DiagramInfo } from './DiagramInfo';`
    - Remove DiagramInfo JSX from render (lines 578-582): the `<DiagramInfo ... />` element inside the flex container. The surrounding `<div className="flex flex-1 overflow-hidden">` stays; it just contains the diagram panel div now.
    - Update DiagramControls props (lines 550-563): Remove all props that were removed from DiagramControlsProps interface. The call becomes:
      ```tsx
      <DiagramControls
        isGenerating={isGenerating}
        onRegenerate={handleRegenerate}
        onForceRegenerate={handleForceRegenerate}
      />
      ```
    - Run `npm run typecheck` — this will confirm all prop threading is clean.

    **File 4 — VisualMapTab.tsx (UICL-01):**
    - DELETE the entire last return block (lines 604-985) — the settings configuration page. This is the fallback return at the end of the component function.
    - After deletion, the component's render flow becomes:
      1. showApiKeyModal -> return API key modal (lines 497-551)
      2. viewMode === 'diagram' && (diagram || svgContent) && metadata -> return DiagramViewer (lines 557-574)
      3. currentState === 'never_generated' && !diagram -> return GeneratePromptCard (lines 577-587)
      4. currentState === 'generating' && !diagram -> return generating spinner (lines 589-602)
      5. NEW FALLBACK: Add a simple fallback return after the generating block that returns the GeneratePromptCard (same as case 3). This handles the case where viewMode is 'settings' (initial state) but no stored diagram is found and state is not 'never_generated' yet (e.g., during the brief moment before state loads). Example:
      ```tsx
      // Fallback: show GeneratePromptCard for any unmatched state
      return (
        <div className="flex items-center justify-center h-full bg-gray-900">
          <GeneratePromptCard
            repoName={repository?.name || 'Repository'}
            onGenerate={() => generateDiagram()}
            isGenerating={isGenerating}
          />
        </div>
      );
      ```
    - Change `viewMode` initial state from `'settings'` to `'diagram'` on line 24. Since the 'settings' branch is removed, initializing to 'diagram' makes the intent clear. The viewMode type can be simplified to `'diagram' | 'tree'` or even just `'diagram'` if the 'tree' branch was only accessible from the now-deleted settings page (which it was — the only setViewMode('tree') call was on line 950 inside the deleted block).
    - Remove unused state variables that ONLY served the deleted settings block:
      - `availableContainers` (line 31) — only rendered in settings block lines 701-721
      - `availableComponents` (line 32) — only rendered in settings block lines 738-758
      - `loadingElements` (line 33) — only rendered in settings block lines 696-698 and 733-735
    - Remove the functions that populated those state vars:
      - `fetchAvailableContainers` (lines 152-174) — only called by the useEffect at lines 50-54
      - `fetchAvailableComponents` (lines 176-198) — only called by the useEffect at lines 57-61
    - Remove the useEffects that called those functions:
      - Lines 50-54 (`if (diagramType === 'c4-component'...)`)
      - Lines 57-61 (`if (diagramType === 'c4-code'...)`)
    - Remove unused imports: `Map`, `FileCode`, `GitBranch`, `FolderTree`, `Settings`, `Play`, `FileText` from lucide-react. Keep: `AlertCircle`, `Key`. Verify each against remaining code paths before removing — `AlertCircle` is used in the API key modal error display.
    - Keep: `DiagramStateBadge` import (used in generating spinner), `GeneratePromptCard` import (used in never_generated return).
    - DO NOT remove: `diagramType`, `detailLevel`, `focusArea`, `modelType` state vars — they are still used by `generateDiagram()`. DO NOT remove `elementId` — used by `generateDiagram()` and `loadPersistedDiagram`.
    - DO NOT remove: `isConfigured`, `apiKey`, `showApiKeyModal`, `checkConfiguration`, `handleSetApiKey` — they power the API key modal that stays.
    - Run `npm run typecheck` after changes.

    **Final verification:** Run the full test suite to confirm all Wave 0 tests now pass (GREEN) and existing tests still pass (no regressions).
  </action>
  <verify>
    <automated>cd /Users/johnrusch/Code/reef && npm run typecheck && npm run test:unit -- --reporter=verbose 2>&1 | tail -60</automated>
  </verify>
  <done>
    - RepositoryTabs: No "Beta" badge text, no beta field in Tab interface
    - DiagramControls: No "Diagram Type", "Detail Level", or "Focus Area" controls; only Regenerate + Force Regenerate buttons + confirm dialog remain
    - DiagramViewer: No DiagramInfo sidebar rendered; DiagramControls receives only isGenerating/onRegenerate/onForceRegenerate props
    - VisualMapTab: No settings landing page; component flow is API key modal -> DiagramViewer -> GeneratePromptCard -> generating spinner -> fallback
    - All Wave 0 tests pass (GREEN)
    - All existing tests pass (no regressions)
    - TypeScript compiles clean (npm run typecheck passes)
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` — zero TypeScript errors across entire project
2. `npm run test:unit -- --reporter=verbose` — all tests pass including new Wave 0 tests
3. `npm test` — full test suite passes (unit + integration + e2e)
4. Existing DiagramViewer tests (KeyboardShortcuts, NavigationDrillDown, ChangeBadge, PlantUMLRenderer) still pass
</verification>

<success_criteria>
- Zero "Beta" text rendered in RepositoryTabs
- Zero "Diagram Type" / "Detail Level" / "Focus Area" labels in DiagramControls
- Zero DiagramInfo rendering in DiagramViewer
- Zero settings configuration page in VisualMapTab
- Regenerate + Force Regenerate buttons functional in DiagramControls
- DiagramViewer still renders diagram content with breadcrumbs and panel
- VisualMapTab still renders GeneratePromptCard for never_generated repos
- VisualMapTab still renders API key modal when unconfigured
- TypeScript compiles clean
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-ui-cleanup/15-01-SUMMARY.md`
</output>
